🚀 GitHub'a Güncelleme Komutları
==================================

Web sitesinde değişiklik yaptıktan sonra GitHub'a yüklemek için:

1. Değişiklikleri kontrol et:
   git status

2. Tüm değişiklikleri ekle:
   git add .

3. Commit yap (değişiklik açıklaması yaz):
   git commit -m "Açıklama buraya"

4. GitHub'a gönder:
   git push

Örnek:
   git add .
   git commit -m "Ana sayfa tasarımını güncelledim"
   git push

💡 İpucu: Commit mesajı kısa ve açıklayıcı olsun!

